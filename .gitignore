.DS_Store
._*
__pycache__/
*.pyc

# media/.previews is generated by serve.py, but it ships anyway: GitHub Pages
# is static, so on the published copy there is nothing to generate it, and the
# map would hand twelve 80MB videos to the browser instead of six-second clips.

# working notes, not for the repo
CONTEXT.md
